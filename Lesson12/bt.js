// Cho arr = [1,54,6,7] để tạo ra một newArr có newArr[i] = arr[i] + 5
const arr = [1, 54, 6, 7];
const newArr = arr.map((value) => value + 5);
// hàm filter
console.log(newArr);
// for (let element of arr) {
// 	newArr.push(element + 5);
// }
